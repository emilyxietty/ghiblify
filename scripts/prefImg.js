// // get the div element that contains the source name
// var sourceDiv = document.querySelector("[data-source='" + name + "']");

// // add a click event listener to the span element inside the div
// sourceDiv.querySelector("span").addEventListener("click", function() {
//   // filter the backlist to get only the images for this source
//   var images = window.newTab.backlist.filter(function(image) {
//     return image.source === name && image.type === "image";
//   });

//   // create an array of image URLs
//   var imageUrls = images.map(function(image) {
//     return image.link;
//   });

//   // show the images in a modal or some other UI element
//   // ...
// });
